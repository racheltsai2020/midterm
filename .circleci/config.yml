version: 2.1

orbs:
  python: circleci/python@2.1.1


jobs:
  build-and-test:
    docker:
      - image: bikeimage:tag
      working_directory: /app
          
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Start server
          command: npm start &
      - run:
          name: Run tests
          command: |
            npm test
workflows:
  sample: 
    jobs:
      - build-and-test